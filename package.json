{
  "name": "meteor-werewolves",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "deploy": "eval $(ssh-agent) && mup deploy && ssh-agent -k"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0"
  }
}
