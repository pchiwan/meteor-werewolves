{
  "name": "meteor-werewolves",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "deploy": "eval $(ssh-agent) && cd .deploy && mup deploy --verbose && ssh-agent -k"
  },
  "dependencies": {
    "babel-runtime": "^6.26.0",
    "bcrypt": "^5.0.0",
    "core-js": "^2.5.1",
    "meteor-node-stubs": "~0.2.0"
  }
}
